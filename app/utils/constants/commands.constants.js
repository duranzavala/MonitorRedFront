const CONTROLLERS = {
    AUTHENTICATE_CONTROLLER: 'auth',
    USER_CONTROLLER: 'user',
};

const COMMANDS = {
    SIGNIN_COMMAND: 'signIn',
    SIGNUP_COMMAND: 'signUp',
};

export {
    CONTROLLERS,
    COMMANDS,
};
